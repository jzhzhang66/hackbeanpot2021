const {getPics} = require('./spotify')
const {getBooksByTitle} = require('./googlebooks');
const { analyzetone } = require('./analyzetone');

//getPics()

//getBooksByTitle('hunger games')


analyzetone('Today I woke up and started crying because my dog died. But then, it came back to life so then I started smiling.')







